export default function login(){
    return <h1>login</h1> /* (nombreCarpeta) -> rutas agrupadas ya que son parte de un mismo proceso significa que vamos a agrupar una ruta de carpetas o de direcciones sin tener que colocar antes /cuenta/login sino que queda /login */
}